{
  "name": "@compr/opscontext-chrome",
  "version": "0.1.4",
  "private": true,
  "description": "OpsContext Browser Capture \u2014 streams Claude.ai and ChatGPT prompt/response events to your local OpsContext audit log.",
  "license": "BSL-1.1",
  "author": {
    "name": "PROD LLC (operating brand of CSS LLC, Swiss, est. 2005)",
    "email": "yannick@compr.ch",
    "url": "https://github.com/FASTPROD/ContextEngine/blob/main/docs/about.md"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/FASTPROD/ContextEngine.git",
    "directory": "chrome-extension"
  },
  "scripts": {
    "build": "node scripts/sync-manifest-version.mjs && tsc && node scripts/bundle-content.mjs && node scripts/copy-static.mjs",
    "watch": "tsc --watch",
    "clean": "rm -rf dist",
    "package": "npm run clean && npm run build && cd dist && zip -r ../opscontext-chrome-$npm_package_version.zip ."
  },
  "devDependencies": {
    "@types/chrome": "^0.0.297",
    "@types/node": "^20.0.0",
    "esbuild": "^0.27.0",
    "typescript": "^5.4.0"
  }
}
